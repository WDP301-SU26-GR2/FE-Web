/**
 * Internal helper types used across the app.
 * Not exported from a barrel — import directly:
 *   import type { UnknownObject } from '~/shared/types/utilities'
 */

export type UnknownObject = Record<string, unknown>