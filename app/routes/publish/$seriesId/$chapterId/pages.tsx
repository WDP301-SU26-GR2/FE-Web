import { PublicationPagesView } from '~/features/mangaka/publication/publication-pages-view'

/** `/publish/:seriesId/:chapterId/pages` — composite page reader. */
export default function PublishChapterPagesRoute() {
  return <PublicationPagesView />
}
