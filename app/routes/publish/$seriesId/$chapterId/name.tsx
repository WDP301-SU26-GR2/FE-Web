import { PublicationNameView } from '~/features/mangaka/publication/publication-name-view'

/** `/publish/:seriesId/:chapterId/name` — storyboard (Name) editor. */
export default function PublishChapterNameRoute() {
  return <PublicationNameView />
}
